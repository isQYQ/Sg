//state
const initialState = {
}

//type

//reducer
export default (state=initialState,action)=>{
    switch (action.type) {
        default:
            return state;
    }
}

//同步action


//异步action